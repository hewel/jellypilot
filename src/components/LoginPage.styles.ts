import { css } from '@styled-system/css';

export const shell = css({
  position: 'relative',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  overflowY: 'auto',
  py: '10',
});

export const footer = css({
  mt: '8',
});
